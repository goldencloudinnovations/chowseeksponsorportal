{
  "name": "chowseek-restaurant-portal",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && npx serve dist",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.8.3"
  },
  "homepage": "https://sponsors.chowseek.com/"
}
