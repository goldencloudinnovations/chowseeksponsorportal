# The hosted Chowseek project supplies the Supabase admin client to Edge Functions
# through the same @supabase/server wrapper used by the existing Chowseek functions.
# It already has STRIPE_API_KEY. The restaurant billing meter/price are provisioned
# by the secure usage-sync function, and the restaurant webhook secret is stored in
# Supabase Vault rather than source-controlled environment files.
PORTAL_URL=https://sponsor.chowseek.com/
